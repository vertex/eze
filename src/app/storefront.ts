import { StorefrontUnfinished } from './storefront-unfinished'
export class Storefront extends StorefrontUnfinished {
    clientId: string;
    readOnly: boolean;
    name: string;
    expires: string;
}