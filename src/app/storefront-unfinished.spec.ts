import { StorefrontUnfinished } from './storefront-unfinished';

describe('StorefrontUnfinished', () => {
  it('should create an instance', () => {
    expect(new StorefrontUnfinished()).toBeTruthy();
  });
});
